{{#include ../include/header012.md}}

# Normal Maps
Each pixel of a normal map is a vector that represents the normal (the direction that the surface is facing) at that point.  
This allows making textures appear to have bumps and grooves without actually changing the geometry of the mesh.

TODO: manual impl of normal maps?  
(Typically you should not do that..)